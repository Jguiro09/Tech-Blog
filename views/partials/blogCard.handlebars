<main>
    <div class="container mt-3">
        <div class="card">
            <div class="card-header">
                <h3 class = "myCardHeaderText">{{title}}</h3>
            </div>
            <div class="card-body">
                {{text}}
            </div>
        </div>
    </div>
</main>